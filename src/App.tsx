import { PrimaryButton } from "./components/PrimaryButton";

export const App = () => {
  return (
    <div>
      <h1>Hello world</h1>

      <PrimaryButton actionType="deleted"></PrimaryButton>
    </div>
  );
};
